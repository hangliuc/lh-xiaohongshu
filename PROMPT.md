# 小红书基金图文笔记生成 Prompt

## 使用方式

将以下 prompt 复制到对话中，替换 `{{变量}}` 部分即可生成任意基金的小红书风格图文笔记。

---

## Prompt 正文

```
请帮我生成基金 {{基金代码}} 的小红书图文笔记，要求如下：

### 一、数据获取

1. 从天天基金（eastmoney）抓取以下数据：
   - 基金主页：名称、代码、类型、风险等级、规模、基金经理、成立日期、管理人、净值、收益率（近1月/3月/6月/1年/3年/成立来）
   - 基金经理页：历任经理任期、当前经理、个人简历
   - 近4年季度持仓（Top10 重仓股，含代码、名称、占净值比）
2. 派生分析：
   - 核心持仓稳定度：最近4季 Top10 取交集，得到"连续4季都在"的核心股
   - 持仓变动：最新季 vs 上一季的新进/退出/加仓/减仓
   - Top3/Top10 集中度趋势（4季）
   - 地区分布（A股 vs 美股/港股）
3. 数据获取时间标注为：{{日期，格式 YYYY.MM.DD}}

### 二、输出文件结构

在 `{{文件夹名}}/` 目录下生成：
- `fetch.py` — 数据抓取脚本
- `data.json` — 抓取结果 + 分析数据
- `{{PREFIX}}.html` — 5页小红书图文的 HTML 源文件
- `shot.js` — Puppeteer 批量截图脚本
- `{{PREFIX}}_01.png` ~ `{{PREFIX}}_05.png` — 5张 1080×1440 截图

### 三、视觉风格规范（深色奠华金）

- 画布尺寸：1080×1440px（小红书标准竖图）
- 配色体系：
  - 背景：深蓝黑渐变 `#05060b → #0b1020 → #070a14`
  - 主色调：奠华金 `#d4b26a`，柔金 `#f2e9d2`
  - 涨色：`#ef3b3b`（红），跌色：`#17b26a`（绿）
  - 文字：`#e7ecf3`（主），`#8a93a6`（辅）
- 字体：
  - 中文标题：Noto Serif SC（宋体衬线），font-weight 900
  - 英文/数字：DM Serif Display（衬线数字）
  - 英文辅助：Cormorant Garamond（斜体衬线）
- 装饰元素：
  - 四角金色 L 型角标（border 1.5px）
  - 页码右下角（DM Serif Display）
  - 水印左下角（Cormorant Garamond italic）
  - 底部免责声明（绝对定位，13px，半透明）
- 卡片内组件：
  - 圆角卡片（border-radius 24~28px）
  - 金色虚线边框（dashed）用于引用/总结区
  - 渐变背景 + 半透明边框
  - 数字用大号 DM Serif Display，单位用小号

### 四、5页内容结构

**第1页 · 封面**
- 顶部：英文品牌名 + 中文基金简称
- 中部：基金全名（渐变金色大字）+ 代码 + 经理名
- 3个关键词标签（圆角胶囊）
- 底部三格数据：近1年收益 / 近3年或6月收益 / 成立来回报
- 引导语："← 滑动查看 · XXX的持仓密码 →"

**第2页 · 近四季持仓演化**
- 4列并排，每列一个季度（最新季用红色边框高亮）
- 每列展示 Top8 持仓（股票名 + 占比%）
- 核心股（连续4季在Top10的）用金色高亮边框标记
- 图例说明高亮逻辑
- 底部总结：一句话概括持仓变化趋势

**第3页 · 基金经理名片**
- 头像区（圆形金色边框 + 姓氏 fallback）
- 姓名 + 英文角色 + 公司职位
- 4格数据：任职回报 / 任职时长 / 管理规模 / 从业年限
- 简历摘要（虚线框）
- 投资风格三个关键词（编号 Ⅰ Ⅱ Ⅲ）

**第4页 · 交易习惯分析**
- 2×2 四格洞察卡片：
  - 核心持仓稳定度（X/10）
  - 集中度/换手率
  - 地区分布
  - 每季换仓数量
- Top10 集中度 4季趋势条（带箭头方向）
- 底部习惯总结（虚线框）

**第5页 · 下一期持仓推测**
- 主推测结论（红色渐变框，大字）
- 三条推理依据（编号 Ⅰ Ⅱ Ⅲ，金色圆角编号）
- 风险提示（绿色边框）
- 结束引导："END · 关注获取下季复盘"

### 五、小红书标题

生成一个小红书单帖标题，要求：
- 字数控制在 20 字以内
- 包含基金名称关键词
- 有吸引力/悬念感
- 示例格式：`XXX基金021842｜成立翻倍的秘密`

### 六、关键规则

1. 所有页面底部必须有免责声明："数据获取 {{日期}} · 本内容仅作信息分享，不构成任何投资建议；如有侵权请联系删除"
2. 持仓演化页的高亮逻辑：**连续4季都出现在 Top10 的股票**才高亮（取交集）
3. 如果基金有多位共管经理，标题和内容都要体现全部经理
4. 每页内容必须控制在 1440px 高度内，不能溢出（用 puppeteer 验证）
5. 换手率数据直接引用天天基金公开披露的年报数据，不自行计算
6. 截图使用 deviceScaleFactor: 2（2倍清晰度）
7. shot.js 使用 puppeteer，通过 `#p1` ~ `#p5` 选择器逐页截图
```

---

## 变量说明

| 变量 | 说明 | 示例 |
|------|------|------|
| `{{基金代码}}` | 6位基金代码 | `021842` |
| `{{日期}}` | 数据获取日期 | `2026.05.05` |
| `{{文件夹名}}` | 输出目录名 | `guofu_qdii` |
| `{{PREFIX}}` | 文件名前缀（大写） | `GUOFU` |

---

## 补充说明

- 如果需要调整页面顺序（比如持仓放第2页、经理放第3页），只需修改 HTML 中的 `id="p2"` / `id="p3"` 和对应页码数字即可
- 如果某只基金成立时间短（不足3年），收益率展示可灵活替换为近6月/近1月等
- 风格可扩展：如需浅色版/其他配色，修改 `:root` 变量即可
